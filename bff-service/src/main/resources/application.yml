server:
  port: 8080

spring:
  application:
    name: bff-service
  mvc:
    problemdetails:
      enabled: true
  profiles:
    default: dev
    group:
      dev:  [backend-dev]
      test: [backend-test]
      prod: [backend-prod]
  config:
    import:
      - "backend.yaml"
