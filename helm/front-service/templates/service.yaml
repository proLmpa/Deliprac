apiVersion: v1
kind: Service
metadata:
  name: front-service
  namespace: baemin
  labels:
    app: front-service
spec:
  type: NodePort
  selector:
    app: front-service
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30000
