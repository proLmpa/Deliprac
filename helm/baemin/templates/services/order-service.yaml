apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: baemin
  labels:
    app: order-service
    {{- include "baemin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 80
      targetPort: 8083
