apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: baemin
  labels:
    app: notification-service
    {{- include "baemin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 80
      targetPort: 8084
