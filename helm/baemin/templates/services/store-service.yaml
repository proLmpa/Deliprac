apiVersion: v1
kind: Service
metadata:
  name: store-service
  namespace: baemin
  labels:
    app: store-service
    {{- include "baemin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: store-service
  ports:
    - name: http
      port: 80
      targetPort: 8082
