apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: baemin
  labels:
    app: ai-service
    {{- include "baemin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: ai-service
  ports:
    - name: http
      port: 80
      targetPort: 8085
