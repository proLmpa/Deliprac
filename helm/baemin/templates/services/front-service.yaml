apiVersion: v1
kind: Service
metadata:
  name: front-service
  namespace: baemin
  labels:
    app: front-service
    {{- include "baemin.labels" . | nindent 4 }}
spec:
  type: NodePort
  selector:
    app: front-service
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30000
