global:
  imageRegistry: oci.bitnami.com
  security:
    allowInsecureImages: true

kafka:
  replicaCount: 1
  kraft:
    enabled: true
  listeners:
    client:
      protocol: PLAINTEXT
  persistence:
    size: 5Gi
  provisioning:
    enabled: false