images:
  registry: prolmpa
  tag: "a137fbda094f"

replicas: 0
javaOpts: "-Xms128m -Xmx512m"

secrets:
  anthropicKey: "changeme"
  hmacAi:       "changeme"
  hmacStore:    "changeme"
  hmacOrder:    "changeme"
