server:
  port: 8083

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5435/orderdb
    username: order_svc
    password: orderpass
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: "baemin-jwt-secret-key-must-be-at-least-32-characters-long"

store-service:
  url: "http://localhost:8082"
