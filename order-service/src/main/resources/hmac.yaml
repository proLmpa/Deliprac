---
spring:
  config:
    activate:
      on-profile: "hmac-dev"
bff.hmac:
  secret: "baemin-bff-order-hmac-secret-key-32chars+"
  window-ms: 30000
---
spring:
  config:
    activate:
      on-profile: "hmac-test"
bff.hmac:
  secret: "baemin-bff-order-hmac-secret-key-32chars+"
  window-ms: 30000
---
spring:
  config:
    activate:
      on-profile: "hmac-prod"
bff.hmac:
  secret: ${BFF_HMAC_ORDER_SECRET}
  window-ms: 30000
