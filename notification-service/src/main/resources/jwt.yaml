---
spring:
  config:
    activate:
      on-profile: "jwt-dev"
jwt:
  secret: "baemin-jwt-secret-key-must-be-at-least-32-characters-long"
  expiration-ms: 86400000
---
spring:
  config:
    activate:
      on-profile: "jwt-test"
jwt:
  secret: "baemin-jwt-secret-key-must-be-at-least-32-characters-long"
  expiration-ms: 86400000
---
spring:
  config:
    activate:
      on-profile: "jwt-prod"
jwt:
  secret: ${JWT_SECRET}
  expiration-ms: 86400000
