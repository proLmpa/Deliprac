---
spring:
  config:
    activate:
      on-profile: "pgsql-dev"
  datasource:
    url: jdbc:postgresql://localhost:5436/notificationdb
    username: notif_svc
    password: notifpass
---
spring:
  config:
    activate:
      on-profile: "pgsql-test"
  datasource:
    url: jdbc:postgresql://localhost:5436/notificationdb
    username: notif_svc
    password: notifpass
---
spring:
  config:
    activate:
      on-profile: "pgsql-prod"
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
