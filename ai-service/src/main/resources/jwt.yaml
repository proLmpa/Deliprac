---
spring:
  config:
    activate:
      on-profile: "jwt-dev"
jwt:
  secret: "baemin-jwt-secret-key-must-be-at-least-32-characters-long"
---
spring:
  config:
    activate:
      on-profile: "jwt-prod"
jwt:
  secret: ${JWT_SECRET}
