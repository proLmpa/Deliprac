apiVersion: v1
kind: Service
metadata:
  name: store-service
  namespace: baemin
spec:
  type: NodePort
  selector:
    app: store-service
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
