apiVersion: v1
kind: Service
metadata:
  name: front-service
  namespace: baemin
spec:
  type: NodePort
  selector:
    app: front-service
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000
