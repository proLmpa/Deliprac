apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: baemin
spec:
  type: NodePort
  selector:
    app: notification-service
  ports:
    - port: 8084
      targetPort: 8084
      nodePort: 30084
